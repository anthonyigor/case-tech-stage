name: case-stage

services:
  db:
    container_name: case-db
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: processes
      POSTGRES_USER: master
      POSTGRES_PASSWORD: senha_1234_stage_test
    ports:
      - "5433:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data: